<Window x:Class="ZoneRouter.UI.ModeToggleBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ZoneRouter Bar"
        Width="280" Height="54"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Border Background="#DD1A1A2E" CornerRadius="10"
            BorderBrush="#44FFFFFF" BorderThickness="1">
        <Grid Margin="8,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 모드 전환 버튼 -->
            <Button x:Name="BtnToggle" Grid.Column="0"
                    Width="100" Height="34"
                    Background="#2196F3" Foreground="White"
                    FontWeight="Bold" FontSize="12"
                    BorderThickness="0"
                    Content="존 모드 ON"
                    Click="BtnToggle_Click"
                    Cursor="Hand"/>

            <!-- 불투명도 슬라이더 -->
            <StackPanel Grid.Column="1" Orientation="Horizontal"
                        VerticalAlignment="Center" Margin="8,0">
                <TextBlock x:Name="OpacityLabel" Text="🌑"
                           Foreground="White" FontSize="13"
                           VerticalAlignment="Center" Margin="0,0,4,0"/>
                <Slider x:Name="OpacitySlider"
                        Width="90" Minimum="0" Maximum="0.9"
                        Value="0.55" VerticalAlignment="Center"
                        ValueChanged="OpacitySlider_ValueChanged"/>
            </StackPanel>

            <!-- 드래그 + 닫기 -->
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <TextBlock Text="⠿" Foreground="#88FFFFFF" FontSize="18"
                           VerticalAlignment="Center" Margin="0,0,6,0"
                           Cursor="SizeAll"
                           MouseLeftButtonDown="DragHandle_MouseDown"/>
                <Button Content="✕" Width="22" Height="22"
                        Background="Transparent" Foreground="#88FFFFFF"
                        BorderThickness="0" FontSize="12"
                        Click="BtnClose_Click" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
