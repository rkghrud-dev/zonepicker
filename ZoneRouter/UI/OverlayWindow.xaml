<Window x:Class="ZoneRouter.UI.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ZoneRouter Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Left="0" Top="0">

    <Grid>
        <!-- 바탕화면 가리기 배경 -->
        <Rectangle x:Name="DimBackground" Fill="#1A1A2E" IsHitTestVisible="False"/>

        <!-- Zone 색상 + 이름 -->
        <Canvas x:Name="ZoneCanvas" IsHitTestVisible="False"/>

        <!-- 분할선 -->
        <Canvas x:Name="LineCanvas" IsHitTestVisible="False"/>

        <!-- Zone 하단 작업표시줄 -->
        <Canvas x:Name="ZoneBarCanvas" Background="Transparent"/>

        <!-- 선 드래그 핸들들 -->
        <Canvas x:Name="HandleCanvas" Background="Transparent">
            <Canvas.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="세로선 추가 (↕)" Click="AddVLine_Click"/>
                    <MenuItem Header="가로선 추가 (↔)" Click="AddHLine_Click"/>
                </ContextMenu>
            </Canvas.ContextMenu>
        </Canvas>

        <!-- 컨트롤 바 (항상 표시) -->
        <Border x:Name="ControlBar"
                VerticalAlignment="Bottom" HorizontalAlignment="Right"
                Margin="0,0,20,20"
                Background="#DD1A1A2E" CornerRadius="10"
                BorderBrush="#44FFFFFF" BorderThickness="1"
                Width="300" Height="56">
            <Grid Margin="8,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BtnToggle" Grid.Column="0"
                        Width="110" Height="36"
                        Background="#2196F3" Foreground="White"
                        FontWeight="Bold" FontSize="12"
                        BorderThickness="0" Content="존 모드 ON"
                        Click="BtnToggle_Click" Cursor="Hand"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal"
                            VerticalAlignment="Center" Margin="8,0">
                    <TextBlock Text="🌑" Foreground="White" FontSize="13"
                               VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <Slider x:Name="OpacitySlider"
                            Width="80" Minimum="0" Maximum="0.9"
                            Value="0.55" VerticalAlignment="Center"
                            ValueChanged="OpacitySlider_ValueChanged"/>
                </StackPanel>

                <Button Grid.Column="2" Content="✕"
                        Width="24" Height="24"
                        Background="Transparent" Foreground="#88FFFFFF"
                        BorderThickness="0" FontSize="13"
                        Click="BtnExit_Click" Cursor="Hand"/>
            </Grid>
        </Border>
    </Grid>
</Window>
